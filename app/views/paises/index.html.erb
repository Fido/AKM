<h1>Listing paises</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Sigla</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @paises.each do |pais| %>
  <tr>
    <td><%= pais.name %></td>
    <td><%= pais.sigla %></td>
    <td><%= link_to 'Show', pais %></td>
    <td><%= link_to 'Edit', edit_pais_path(pais) %></td>
    <td><%= link_to 'Destroy', pais, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Pais', new_pais_path %>
