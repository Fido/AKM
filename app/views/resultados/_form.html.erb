<%= form_for(@resultado) do |f| %>
  <% if @resultado.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@resultado.errors.count, "error") %> prohibited this resultado from being saved:</h2>

      <ul>
      <% @resultado.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :equipo_id %><br />
    <%= f.text_field :equipo_id %>
  </div>
  <div class="field">
    <%= f.label :respuesta_id %><br />
    <%= f.text_field :respuesta_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
