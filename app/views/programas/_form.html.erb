<%= form_for(@programa) do |f| %>
  <% if @programa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@programa.errors.count, "error") %> prohibited this programa from being saved:</h2>

      <ul>
      <% @programa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :codigo %><br />
    <%= f.text_field :codigo %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
